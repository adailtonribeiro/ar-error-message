module.exports = {
  //extends: ['@commitlint/config-angular'],
  // NOTE: It's used config-conventional because config-angular doesn't allow 'chore' any longer
  extends: ['@commitlint/config-conventional']
  //rules: {}
};
